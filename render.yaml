services:
  - type: web
    name: codedeck
    runtime: node
    buildCommand: npm install
    startCommand: node codedeck.js
    envVars:
      - key: CODEDECK_PORT
        value: 10000
      - key: NODE_ENV
        value: production
